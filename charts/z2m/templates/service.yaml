apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-zigbee2mqtt-service
spec:
  selector:
    app: zigbee2mqtt
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: NodePort
